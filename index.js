/**
 * DeepSeek Harness plugin entry.
 *
 * 0.0.1 is a reserved npm name / installable bundle stub.
 * The proxy-tool implementation lands in a later release.
 */

export const name = 'dsh-mcp-adapter'

export function apply() {
  console.info('[dsh-mcp-adapter] placeholder 0.0.1 loaded — MCP proxy is not implemented yet')
}
